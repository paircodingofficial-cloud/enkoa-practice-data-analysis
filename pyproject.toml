[project]
name = "enkoa-practice-data-analysis"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "ipykernel>=7.3.0",
    "jupyterlab>=4.6.2",
    "kiwipiepy>=0.23.2",
    "matplotlib>=3.11.1",
    "networkx>=3.6.1",
    "numpy>=2.3,<2.5",   # umap-learn 이 쓰는 numba 가 numpy 2.5 를 아직 지원하지 않는다(상한 없으면 llvmlite 0.36 으로 후퇴해 설치 실패)
    "openpyxl>=3.1.5",
    "pandas>=2.3,<3",
    "pingouin>=0.6.1",
    "openai>=1.0",
    "pillow>=10.0",
    "python-dotenv>=1.0",
    "sentence-transformers>=3.0",
    "umap-learn>=0.5.6",
    "scikit-learn>=1.9.0",
    "scikit-posthocs>=0.14",
    "scipy>=1.18.0",
    "seaborn>=0.13.2",
    "statsmodels>=0.14",
    "wordcloud>=1.9.6",
]
