# 가설검정·회귀분석 실습 가이드 — 진행 체크리스트

지난 시간에 **신뢰구간으로 주장을 판단**한 것을 이제 **귀무가설·p-value 를 갖춘 정식 가설검정**으로 공식화하고, **상관 → 회귀**로 나아가 데이터로 의사결정하는 하루입니다.
아래 체크박스를 순서대로 지워 나가세요. 완료 기준은 **자가채점 셀에서 `✅ 통과!`**(서술형은 서술 셀 작성, **그래프 문제는 "완성 그래프(정답)" 와 같은 모양으로 그리기**).

> 통계는 별도 이론 수업이 없으므로 교안에 이론을 충분히 담았습니다.
>
> 교안은 강사 **시연**을 **자동차 연비(mpg) + 훈련 기록**으로 보고, **함께 따라하기는 시연과 다른 데이터 — 고객센터 상담 기록**(`callcenter_calls.csv`·`callcenter_agents.csv`)으로 직접 연습합니다. 과제는 또 레벨마다 다른 데이터 — **LV1 타이타닉 · LV2 펭귄 · LV3 웹 A/B 실험** — 로 풉니다. 데이터가 계속 달라지므로 배운 검정을 **새 데이터에 적용**하는 훈련이 됩니다. 강사용 완성본은 `정답/` 폴더.

## 0. 준비 (수업 시작 전, 약 5분)

- [ ] **필요한 라이브러리를 설치**하세요 — 터미널에서 아래를 실행합니다(환경 구축도 실습의 일부입니다).

```bash
uv add jupyter pandas numpy matplotlib seaborn pingouin scipy statsmodels   # day09 (jupyter 안에 VS Code 커널용 ipykernel 포함)
```
- [ ] **VS Code** 로 노트북을 열고, 오른쪽 위에서 커널로 **Python 3 (ipykernel)** — 방금 만든 `.venv` — 선택
- [ ] `교안_01` 맨 위 `# [제공 코드]` 셀이 에러 없이 실행되는지 확인(`import statsmodels.formula.api as smf` 포함)
- [ ] `data/` 폴더에 CSV 8종이 있는지 확인
      (시연: mpg·train_before_after / 따라하기: callcenter_calls·callcenter_agents / 과제: titanic·penguins·ab_data·study_scores)

> 검정·회귀는 **가정 점검(정규성·등분산)** 이 먼저입니다. 결과의 p-value 만 보지 말고 **효과크기**도 함께 보세요.

## 1. 교안_01 가설검정 (강사와 함께)

- [ ] 1 가설검정 프레임 — H₀/H₁·유의수준 α·검정통계량·p-value(귀무분포 꼬리)·1종/2종 오류
- [ ] 🔧 주력 도구 **Pingouin**(`pg`) 소개 — 검정+효과크기+신뢰구간을 표 하나로 (대안: scipy/statsmodels)
- [ ] 2 가정 점검 — 정규성 `pg.normality`·`pg.qqplot`(Q-Q), 등분산 `pg.homoscedasticity`
- [ ] 3 t-검정 3종 — 1표본 `pg.ttest(x, 기준값)` / 대응 `pg.ttest(a, b, paired=True)` / 독립 `pg.ttest(a, b, correction=)`(Student vs Welch) + Cohen's d(자동)
- [ ] 4 모수 vs 비모수 — 선택 기준, `pg.mwu` + RBC·CLES, 그리고 나머지 두 짝 **`pg.wilcoxon`(대응)·`pg.kruskal`(3집단+)** 직접 실행
- [ ] 5 분산분석(ANOVA) — `pg.anova`(np2=η²) + 사후검정 `pg.pairwise_tukey`(다중검정 문제) / 이분산이면 `pg.welch_anova`+`pg.pairwise_gameshowell`
- [ ] 6 카이제곱 — 적합도 `scipy.stats.chisquare`(🔧 pg 미지원) / 독립성 `pg.chi2_independence`(Cramér's V 자동) + 조정된 잔차(statsmodels)
- [ ] 7 검정 선택 가이드 — 데이터 유형별 의사결정
- [ ] 🚀 응용 클론코딩 — 교육 전후 **대응표본 t** + 채널별 만족도·재문의율로 **개선안 검증 리포트**
- [ ] ✅ 각 섹션 따라하기 + 바로 확인 퀴즈

## 2. 교안_02 회귀분석 (강사와 함께)

- [ ] 1 상관에서 회귀로 — r → r²(결정계수) → 예측, 상관 ≠ 인과
- [ ] 2 단순 선형회귀 — `smf.ols('y ~ x').fit()`·`.summary()` 읽기(coef·P>|t|·R²), 단순회귀 R² = 상관 r²
- [ ] 3 다중 선형회귀 — 여러 변수, Adj R²·계수 p-value·다중공선성(VIF 개념)
- [ ] 4 회귀 진단 — 잔차 vs 적합값·잔차 Q-Q·Durbin-Watson(독립성)
- [ ] 5 데이터 기반 의사결정 — A/B 테스트(`proportions_ztest`·Cohen's h·`proportion_confint`·표본크기 `NormalIndPower`)
- [ ] 6 종합 워크플로와 한계 — 통계적 유의 ≠ 실질적 유의, 상관 ≠ 인과
- [ ] 🚀 응용 클론코딩 — 상관 → 다중회귀 → **표준화 계수로 영향력 비교** → 진단(VIF·DW)까지 **만족도 예측 리포트**
- [ ] ✅ 각 섹션 따라하기 + 바로 확인 퀴즈

## 3. 과제 (자기 주도, 자가채점 `✅ 통과!` 기준)

### 과제_LV1_기초 — 15문 (권장 120분 내외) · **타이타닉(titanic)** 데이터 (5·6번만 학습점수 companion)

- [ ] 1 데이터 살펴보기 (서술형)
- [ ] 2 정규성 검정 (Shapiro-Wilk) — 판단 서술 포함
- [ ] 3 1표본 t-검정
- [ ] 4 독립 2표본 t-검정 — **등분산 점검 후 Student/Welch 스스로 선택** + Cohen's d
- [ ] 5 대응표본 t-검정 (paired, 학습 프로그램 전/후 점수)
- [ ] 6 **Wilcoxon 부호순위** (`pg.wilcoxon`) — 5번의 비모수 짝, 두 결론이 갈리는 이유 서술
- [ ] 7 **정규성 점검 후 검정 선택** (정규성이 깨지면 순위 기반 `pg.mwu` 로)
- [ ] 8 일원분산분석 (One-way ANOVA)
- [ ] 9 사후검정 (Tukey HSD)
- [ ] 10 카이제곱 독립성 검정 + Cramér's V
- [ ] 11 카이제곱 적합도 검정
- [ ] 12 정규성 시각화 — Q-Q Plot (그래프)
- [ ] 13 **미니 통합 — 가정 점검 → 검정 선택 → 실행 → 효과크기 해석**
- [ ] 14 검정 결과 해석하기 (서술형)
- [ ] 15 어떤 상황에 어떤 검정? (서술형)

### 과제_LV2_응용 — 10문 (권장 135분 내외) · **펭귄(penguins)** 데이터

- [ ] 1 데이터 관찰 (서술)
- [ ] 2 정규성·등분산 점검과 검정 선택 (가정 점검 + 서술)
- [ ] 3 종별 체중 차이 — 일원분산분석 + η²
- [ ] 4 어느 종끼리 다른가 — Tukey 사후검정 (표)
- [ ] 5 **Kruskal-Wallis** (`pg.kruskal`) — 3번 ANOVA 의 비모수 짝, 중앙값과 함께 해석 + 서술
- [ ] 6 종과 서식지는 독립인가 — 카이제곱 + Cramér's V
- [ ] 7 물갈퀴 길이로 체중 예측 — 단순 선형회귀 (검정 + 그래프)
- [ ] 8 변수를 더하면 나아질까 — 다중 선형회귀
- [ ] 9 회귀가 타당한가 — 잔차 진단 (그래프)
- [ ] 10 인사이트 리포트 (종합 서술)

### 과제_LV3_통합 — 2문 (권장 120분+) · **웹 A/B 실험(ab_data)** 데이터

- [ ] 1 A/B 전환율 분석 (로드·구조 → 정제(불일치·중복 제거) → 그룹별 전환율 → 막대그래프 → z-검정·Cohen's h → 95% 신뢰구간 → 의사결정 서술)
- [ ] 2 대용량 표본과 실질적 유의성 (정제본 재사용 → 전환율 차이·상대변화 → 검정·효과크기 → 비교 그래프 → 통계적 vs 실질적 유의성 서술 → 표본크기 산정)

## 막혔을 때

- [ ] 문제 맨 위 `pd.read_csv(...)` 셀부터 다시 실행. 각 문제는 원본 재로딩 또는 `.copy()` 로 시작.
- [ ] 결측치가 있는 열(titanic age·penguins 체중)은 검정 전에 `dropna()`
- [ ] `pg.ttest(a, b)` 의 기본은 Student(`correction=False`) — 등분산을 확신 못 하면 **Welch(`correction=True`)**. 대응표본은 `pg.ttest(a, b, paired=True)`(차이값 정규성만 확인). 결과 표의 `cohen_d`·`CI95`·`power` 를 함께 보기
- [ ] 회귀는 `smf.ols('y ~ x1 + x2', data=df).fit()` → `print(model.summary())` 로 계수·p-value·R² 확인
- [ ] 그래프 문제는 **자동 채점이 없습니다** — 완성 그래프 스크린샷과 같은 모양으로 그리세요
- [ ] p-value 만 보지 말고 **효과크기**(Cohen's d·η²·Cramér's V·Cohen's h)로 "얼마나 큰 차이인지"도 판단
- [ ] 그래도 막히면 강사에게 — 정답 노트북은 스스로 푼 뒤에만 열기

## 오늘의 완주 기준

- [ ] 교안 2권의 따라하기·퀴즈 전부 실행
- [ ] 정량 문제는 자가채점 `✅ 통과!`, 그래프 문제는 완성 그래프처럼 그리기 (LV1 15 / LV2 10 / LV3 2문)
- [ ] 데이터 상황을 보고 **어떤 검정을 쓸지 스스로 고르고**, p-value 와 **효과크기**로 결론을 낼 수 있다
- [ ] 회귀로 **예측·설명**을 하고, 통계적 유의성과 **실질적 유의성**을 구분해 데이터로 의사결정할 수 있다
